# 
neofetch

# applications
code
zathura
okular
# has clipboard option, in case of mint, it automatically uses this one instead of vim.tiny (or something like this one)
vim-gtk3

# python stuff
python3-pip
pip install virtualenv 

# i3 stuff
i3
i3blocks
rofi

# xournal++ (https://xournalpp.github.io/installation/linux/)
sudo add-apt-repository ppa:apandada1/xournalpp-stable
sudo apt update
sudo apt install xournalpp

# from the snap
slack
